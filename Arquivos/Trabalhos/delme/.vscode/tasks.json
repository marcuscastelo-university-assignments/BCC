{
    "tasks": [
        {
            "type": "shell",
            "label": "gcc build active file",
            "command": "/usr/bin/gcc",
            "args": [
                "-g",
                "${workspaceFolder}/src/main.c",
                "${workspaceFolder}/src/outro/outro.c",
                "-o",
                "${workspaceFolder}/main",
                "-I${workspaceFolder}/headers/outro"
            ],
            "options": {
                "cwd": "/usr/bin"
            }
        }
    ],
    "version": "2.0.0"
}